import scipy.stats
import numpy as np

# fix seed to get the same result as examples on the internet
# np.random.seed(12345678)

# t : float or array - The calculated t-statistic
# prob : float or array - The two-tailed p-value

# means of the statistics
# TODO: finish script for student_t analysis
tao = [
zori = 
# axis : int, optional - Axis can equal None (ravel array first), or an integer (the axis over which to operate on a and b)
[t, prob] = scipy.stats.ttest_ind(tao, zori, axis=0)
